spring:
  application:
    name: vehicleBookingSystem

  datasource:
    url: jdbc:mysql://localhost:3306/vehicle_booking_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
